- content_for :script do
	= javascript_include_tag "audiojs/audio.js"

	:javascript
		audiojs.events.ready(function() {
			var as = audiojs.createAll();
		});

%p#notice= notice

%p
	%b Name:
	= @song.name
%p
	= audio_tag @song.mp3, :src => @song.mp3
%p
	%b Album:
	= @song.product.name

= link_to 'Edit', edit_admin_product_songs_url(@product, @song)
\|
= link_to 'Back', admin_product_songs_url(@product)